
spring:
  profiles:
    active: prod
  datasource:
    url: jdbc:mysql://localhost:3306/tan?characterEncoding=UTF8&socketTimeout=60000
    name: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
